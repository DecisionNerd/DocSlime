{
  "name": "docslime-docs",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docmd dev",
    "build": "docmd build && node scripts/check-rendered-line-breaks.mjs",
    "preview": "npx serve site"
  },
  "dependencies": {
    "@docmd/core": "^0.8.10"
  }
}
